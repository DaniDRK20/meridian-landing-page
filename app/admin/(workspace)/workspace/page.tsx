"use client";import {useWorkspace,WorkspaceState} from "../../workspace-store";
export default function WorkspacePage(){const {tasks,documents,events}=useWorkspace();return <WorkspaceState><div className="workspace-heading"><div><span className="admin-eyebrow">Meridian Workspace</span><h1>Centro operativo</h1><p>Todos los módulos comparten información persistente.</p></div></div><div className="docs-grid">{[["Kanban",`${tasks.length} tareas activas`,"/admin/kanban"],["Backlog",`${tasks.filter(t=>t.status==="Backlog").length} historias`,"/admin/backlog"],["Calendario",`${events.length} eventos`,"/admin/calendario"],["Documentación",`${documents.length} documentos`,"/admin/documentacion"]].map(([title,text,href])=><a className="doc-card" href={href} key={title}><span>◇</span><h2>{title}</h2><p>{text}</p><small>Abrir →</small></a>)}</div><section className="workspace-panel" style={{marginTop:18}}><header><h2>Trabajo reciente</h2></header><div className="delivery-list">{tasks.slice(0,5).map(t=><p key={t.id}><span className="priority-dot"/><b>{t.title}</b><small>{t.status}</small></p>)}</div></section></WorkspaceState>}
