import { team } from "../../workspace-data";
export default function TeamPage(){return <><div className="workspace-heading"><div><span className="admin-eyebrow">Equipo</span><h1>Personas que construyen Meridian</h1><p>Capacidad, foco y disponibilidad del Sprint 04.</p></div></div><div className="team-grid">{team.map(member=><article className="team-card" key={member.name}><div className="team-head"><span className="avatar large">{member.initials}</span><span><h2>{member.name}</h2><p>{member.role}</p></span><i className={member.availability === "Disponible" ? "available" : "focus"}>{member.availability}</i></div><div className="load"><span>Carga de trabajo <b>{member.load}%</b></span><div className="progress"><i style={{width:`${member.load}%`}}/></div></div><dl><div><dt>Sprint</dt><dd>04</dd></div><div><dt>Completadas</dt><dd>{member.done}</dd></div><div><dt>Pendientes</dt><dd>{member.pending}</dd></div></dl></article>)}</div></>}
