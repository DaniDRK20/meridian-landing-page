import { getAdminSession } from "@/lib/admin-session";
export default async function ProfilePage(){const user=await getAdminSession();return <><div className="workspace-heading"><div><span className="admin-eyebrow">Cuenta</span><h1>Perfil administrativo</h1><p>Información mínima asociada a tu sesión segura.</p></div></div><section className="workspace-panel" style={{maxWidth:680}}><div className="team-head"><span className="avatar large">{user?.name.slice(0,2).toUpperCase()}</span><span><h2>{user?.name}</h2><p>{user?.email}</p></span><i className="available">Administrador activo</i></div><div className="settings-list" style={{marginTop:24}}><article><span><b>Rol</b><small>{user?.role}</small></span></article><article><span><b>Seguridad</b><small>Sesión protegida mediante cookie HttpOnly</small></span></article></div></section></>}
